/**
 * Built-in API URL for "production" builds.
 * - Leave empty '' to show the API URL field and use localhost/default (flexible, multi-environment).
 * - Set to your server URL (e.g. 'https://api.yourdomain.com') to hide the API URL field
 *   so users just Log in / Register directly, like on local.
 */
export const BUILTIN_API_URL = '';
