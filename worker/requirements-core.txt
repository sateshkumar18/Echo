# Echo AI Worker – core only (no transcription)
# Python 3.11 or 3.12 required. Python 3.14 is not yet supported by psycopg-binary.
# Create venv with: py -3.12 -m venv .venv

flask>=3.0.0,<4
psycopg[binary]>=3.1.0,<4
ollama>=0.3.0
